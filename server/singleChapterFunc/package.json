{
	"name": "scf",
	"version": "1.0.0",
	"description": "A function to download a single chapter of a fanfiction",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"install": "npx puppeteer browsers install chrome && npm install"
	},
	"author": "Abhinav Gupta",
	"license": "ISC",
	"dependencies": {
		"puppeteer": "^22.10.0"
	}
}
